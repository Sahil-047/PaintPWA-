# Marketing Frontend

This is a standalone, frontend-only advertising website for Paint ERP.

## Files

- `index.html`
- `styles.css`
- `script.js`

## Run locally

Option 1: Open `index.html` directly in browser.

Option 2: Serve with a static server:

```bash
npx serve .
```

Then open the local URL shown in terminal.
